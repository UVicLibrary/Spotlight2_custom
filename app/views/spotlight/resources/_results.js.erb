var radioValue = $("input[name='toggleSelection']:checked").val();
$('#results').html("<%= j (render partial: 'results.html') %>");
  // Allow search but disable add buttons
  if(radioValue == "use-range"){
    $('a.add-doc-id button').prop('disabled', true);
  }
